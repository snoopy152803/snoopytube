# MyTube

A YouTube-style site with a personalised home feed. Every video links to the real
thing on YouTube (thumbnails from `i.ytimg.com`, embedded player), but the
recommendations are computed locally from what you watch, like and subscribe to
on this site — stored in your browser's `localStorage`, nothing leaves your machine.

Open `index.html` or visit the GitHub Pages deployment.
